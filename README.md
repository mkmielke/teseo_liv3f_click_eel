# Generating the EEL File

Use the eel builder tool found here: https://github.com/atmosphereiot/eel-builder

## Usage

    node eelbuilder.js --generate --dir"<directory containing source files>" --dest="<where to save the .eel file>"
